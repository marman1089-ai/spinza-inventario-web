services:
  - type: web
    name: spinza-inventario
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    autoDeploy: true
    healthCheckPath: /select-store
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
